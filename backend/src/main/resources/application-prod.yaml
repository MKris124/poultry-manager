spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:h2:file:${user.home}/Documents/BaromfiMenedzser/poultry_db;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;
    driver-class-name: org.h2.Driver
    username: sa
    password: password

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true